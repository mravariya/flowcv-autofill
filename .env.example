# FlowCV Autofill
FLOWCV_EMAIL=
FLOWCV_PASSWORD=

# Job Application Tracker
ENCRYPTION_KEY=        # python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
GITHUB_TOKEN=          # optional, for sync feature
GITHUB_REPO=           # optional, e.g. username/flowcv-autofill
